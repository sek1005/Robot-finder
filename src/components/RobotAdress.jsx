import React from 'react';

function RobotAdress ({suite,zipcode,city}) {
 
    return (
        <>
        <p>{suite}</p>
        <p>{zipcode}</p>
        <p>{city}</p>
        </>
    )
};
export default RobotAdress;